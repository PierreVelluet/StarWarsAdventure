export enum WelcomingSteps {
  Step0,
  Step1,
  Step2,
  Step3,
}

export enum StepType {
  Introduction,
  Choice,
  Game,
}

export enum SteppingDirection {
  Forward = +1,
  Backward = -1,
}

export enum TransitionType {
  Introduction,
  Choice,
  Game,
}
